import logo from "../assets/img/logo.svg";
const Header = () => {
  return (
    <header>
      <div className="container">
        <img src={logo}></img>
      </div>
    </header>
  );
};

export default Header;
